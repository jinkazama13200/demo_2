class Product {
  constructor(id, name, price, image, type, material, color) {
    (this.id = id),
      (this.name = name),
      (this.price = +price),
      (this.image = image),
      (this.type = type),
      (this.material = material),
      (this.color = color);
  }
}
